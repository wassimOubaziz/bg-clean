{
  "version": 2,
  "builds": [{ "src": "Dockerfile", "use": "@vercel/docker" }],
  "routes": [
    { "src": "/api/(.*)", "dest": "/src/app/api/$1" },
    { "src": "/(.*)", "dest": "/src/app/page.js" }
  ]
}
